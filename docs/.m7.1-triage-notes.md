# P2-M7.1 triage notes (working scratch — Task 15 folds this into REVIEW_2026_07_04.md)

## GitHub-Actions Dependabot PRs (Task 4, absorbed locally 2026-07-04)

| PR | action old→new | taken/deferred | reason |
|----|----------------|----------------|--------|
| #1 | actions/setup-go v5→v6 (v6.5.0) | taken | Breaking changes are node24 runtime (CI-side, runner ≥ v2.327.1 on ubuntu-latest) + improved toolchain selection; `go-version-file: go.mod` + `cache` inputs unchanged. |
| #2 | actions/checkout v4→v7 (v7.0.0) | taken | v5/v6/v7 breaking notes: node24 runtime, creds persisted to a separate file (v6), fork-PR checkout blocked for `pull_request_target`/`workflow_run` (v7) — we only trigger on `push`/`pull_request`, unaffected. |
| #3 | docker/setup-buildx-action v3→v4 (v4.2.0) | taken | Major is a node24 runtime bump; no input changes; we pass no inputs. |
| #4 | golangci/golangci-lint-action v8→v9 (v9.3.0) | taken | v9 gate PASSED: v9.0.0 notes = node20→node24 runtime + additive features (install-only, Module Plugin System); no input removals; v2-format `.golangci.yml` (ours is `version: "2"`) still the supported config; `version: latest` input unchanged; Go version comes from setup-go (workflow uses `go-version-file`), unaffected. v9.1.0–v9.3.0 notes contain no breaking changes. |
| #5 | actions/setup-node v4→v6 (v6.4.0) | taken | v5 breaking = auto package-manager cache detection + node24; v6 breaking = auto-caching limited to npm. We pass `cache: npm` + `node-version-file: .nvmrc` explicitly, so behaviour is pinned regardless. |

All five absorbed as SHA pins with `# vX.Y.Z` comments (Task 4 also SHA-pinned the other 8 action repos at their current major's latest release). PRs #1–#5 auto-close when the operator pushes main.

## Go vulnerability alerts (Task 6, absorbed PR #13 locally 2026-07-05)

| alert | module old→patched→taken | verdict | reason/evidence |
|-------|--------------------------|---------|-----------------|
| x/image TIFF PackBits excessive resource consumption (HIGH, < 0.41.0) | golang.org/x/image v0.38.0→v0.41.0→**v0.43.0** | **unreachable, patched anyway** | `go mod why golang.org/x/image/tiff` → "main module does not need package"; module needed only via `golang.org/x/image/webp` imported by `internal/integration/m5_image_test.go` (test-only); govips does image work via libvips (cgo), its go.mod edge to x/image never reaches the tiff package. Policy: patch regardless. |
| x/net HTML parser DoS (MEDIUM, < 0.55.0) | golang.org/x/net v0.53.0 (indirect)→v0.55.0→**v0.55.0** | **reachable, patched** | `go mod why golang.org/x/net/html` → nova-image/internal/transform → govips/v2/vips → x/net/html/charset → x/net/html — production image-transform path does reach the parser (charset sniffing); no direct x/net import in Nova source. Bumped explicitly; stays indirect in go.mod. |

PR #13 (Go minor/patch group, 10 modules) absorbed with two holds: **kubo held at v0.41.0** (v0.42.0 requires `go 1.26.4`; go.mod pins 1.26.2 per toolchain policy) and **boxo held at v0.39.0** (boxo ≥0.40 forces go-libp2p-kad-dht v0.40.0, whose new provider `Stats(ctx)` API does not compile against kubo v0.41.0; kad-dht taken at v0.39.2, the newest compatible patch). Remaining 8 direct movements taken at PR versions (no newer same-minor patches existed as of 2026-07-05), except **x/crypto uplifted one further to v0.52.0**: govulncheck flagged five reachable x/crypto/ssh vulns at v0.51.0 (GO-2026-5013/5017/5018/5019/5020, all fixed in v0.52.0). Post-bump govulncheck: only GO-2024-3218 remains (kad-dht content censorship, **Fixed in: N/A** — no fixed release exists; pre-existing at v0.39.1, affects all versions; accepted/known, not introduced by this change). PR #13 auto-closes on push; the kubo/boxo group will re-open — defer to a milestone that lifts the go directive.

## npm vulnerability alerts (Task 7, absorbed PR #14 locally 2026-07-05)

| alert | package old→patched-floor→taken | verdict | reason/evidence |
|-------|----------------------------------|---------|-----------------|
| #31 undici TLS certificate validation bypass via dropped requestTls in SOCKS5 ProxyAgent (HIGH, GHSA-vmh5-mc38-953g, ≥7.23.0 <7.28.0) | undici 7.27.2→7.28.0→**7.28.0** | **dev/test-only, patched anyway** | `npm ls undici` → sole parent chain is `jsdom@29.1.1 → undici@7.28.0`; jsdom is a devDependency of all three workspaces (vitest jsdom test environment). undici never ships in the SPA bundles (browser fetch is native; hermetic-spa gates confirm no runtime network deps). No ProxyAgent/SOCKS5 use anywhere. `npm audit --omit=dev`: 0 vulns. |
| #32 undici cross-user information disclosure via shared cache whitespace bypass (MEDIUM, GHSA-pr7r-676h-xcf6, ≥7.0.0 <7.28.0) | undici 7.27.2→7.28.0→**7.28.0** | **dev/test-only, patched anyway** | Same single jsdom parent chain; the undici shared-cache interceptor is not enabled by jsdom's resource loader; test-only surface. |
| #33 undici HTTP response queue poisoning via keep-alive socket reuse (LOW, GHSA-35p6-xmwp-9g52, ≥7.0.0 <7.28.0) | undici 7.27.2→7.28.0→**7.28.0** | **dev/test-only, patched anyway** | Same jsdom chain; tests fetch nothing remote (vitest runs offline against local fixtures). |
| #34 undici cross-origin request routing via SOCKS5 proxy pool reuse (HIGH, GHSA-hm92-r4w5-c3mj, ≥7.23.0 <7.28.0) | undici 7.27.2→7.28.0→**7.28.0** | **dev/test-only, patched anyway** | Same jsdom chain; no SOCKS5 proxying anywhere in the repo. |
| #36 undici HTTP header injection via Set-Cookie percent-decoding (MEDIUM, GHSA-p88m-4jfj-68fv, ≥7.0.0 <7.28.0) | undici 7.27.2→7.28.0→**7.28.0** | **dev/test-only, patched anyway** | Same jsdom chain; jsdom cookie handling is exercised only against local test HTML. |
| #37 undici Set-Cookie SameSite attribute downgrade via permissive substring matching (LOW, GHSA-g8m3-5g58-fq7m, ≥7.0.0 <7.28.0) | undici 7.27.2→7.28.0→**7.28.0** | **dev/test-only, patched anyway** | Same jsdom chain. NOTE: this alert opened after the Task-7 brief was written (brief said undici ×5; GitHub now shows ×6) — same 7.28.0 floor closes it. |
| #29 esbuild arbitrary file read when running the development server on Windows (LOW, GHSA-g7r4-m6w7-qqqr, ≥0.27.3 <0.28.1) | esbuild 0.28.0→0.28.1→**0.28.1** | **dev-only + wrong OS, patched anyway** | `npm ls esbuild` → sole parent is `vite@8.1.3 → esbuild@0.28.1` (optional peerDep, build toolchain only). Advisory is the esbuild *dev server* on *Windows*; builds/CI run Linux and esbuild never ships in dist. |

All 7 open npm alerts (6 undici + 1 esbuild) share the two floors above; both were reachable within existing parent semver ranges (jsdom pins `undici@^7.25.0`; vite 8.1.x peers `esbuild@^0.27.0 || ^0.28.0`), so **no `overrides` entry was needed** — the existing `@uppy/core → nanoid ^5.1.6` override is untouched (resolves 5.1.16, `overridden` confirmed in `npm ls nanoid`). PR #14's six movements absorbed at the newest same-minor patches as of 2026-07-05: react-dialog ^1.1.18 (PR: 1.1.17), react-dropdown-menu ^2.1.19 (PR: 2.1.18), react-toast ^1.2.18 (PR: 1.2.17), @vitejs/plugin-react ^6.0.3 (= PR), vite ^8.1.3 (PR: 8.1.0), vitest ^4.1.9 (= PR). `npm audit` and `npm audit --omit=dev`: **0 vulnerabilities**. Absorption note: npm would not reconcile the stale workspace-nested lock entries (`web/*/node_modules/{vite,esbuild,@vitejs/plugin-react}` stuck at old versions, `npm ls` ELSPROBLEMS "invalid") after the manifest bumps — known npm-workspaces quirk; fixed by deleting those stale lock entries + dirs and re-running `npm install`, which re-resolved everything hoisted and deduped. PR #14 auto-closes when the operator pushes main.

## npm MAJOR Dependabot PRs (Task 8, triaged 2026-07-05)

Rule applied: take only if the migration is mechanical AND `make web` stays fully green; majors are not beta blockers, broken SPAs are. Attempt order cheapest-first: #7 → #10 → #11 → #9.

| PR | package old→new | taken/deferred | reason |
|----|-----------------|----------------|--------|
| #7 | typescript 5.9.3→6.0.3 (devDep, all 3 workspaces) | **taken** | TS 6.0 is the 5.9-compatible bridge release to the TS 7 native port. One fallout: TS 6 stops implicitly including hoisted transitive `@types` packages, so the vitest setup files' `node:crypto` import (admin + setup) lost types. Fix = the compiler-prescribed pair: `@types/node` ^25.9.1 as explicit devDep (dedupes against the copy vite/vitest already pull) + `"types": ["node"]` in the two tsconfigs. Widget untouched. `make web` fully green. |
| #10 | @tanstack/react-query 4.44.0→5.101.0 (web/admin; web/setup declares but never imports it) | **deferred** | Measured fan-out exceeds the mechanical time-box: 16 compile-breaking mutation `.isLoading`→`.isPending` sites across 4 screens (Keys, Settings, Moderation, BlobDetail) + `keepPreviousData`→`placeholderData: keepPreviousData` (useList.ts) + `refetchInterval` callback signature change (Keys.tsx). Also trips the cache-semantics stop condition: v5 redefines query `isLoading` as `isPending && isFetching`, silently changing what the 7 `loading={query.isLoading}` table props report for gated/disabled queries. Well-understood migration (official codemod exists) — routine currency work post-beta. |
| #11 | react-router-dom 6.30.4→7.17.0 (manifest ^7.17.0, lock resolves 7.18.1; admin + setup) | **taken** | Zero source changes: admin uses only declarative-mode API that survives v7 unchanged (BrowserRouter/Routes/Route/Navigate/NavLink/Outlet, useLocation/useNavigate/useParams/useSearchParams, MemoryRouter in tests); no data routers, no `json()`/`defer()`; the only splat route navigates to an absolute path so the relative-splat-path change is moot. web/setup declares the dep but never imports it (pre-existing dead dep — left for a later cleanup). `make web` fully green. |
| #9 | @uppy/drag-drop 3.1.1→5.1.0 (web/widget) | **deferred** | PR is incoherent as authored: drag-drop@5.1.0 peers on `@uppy/core ^5.2.0`, so a real take is a 4-package family bump (core ^5.2.0, drag-drop ^5.1.0, status-bar ^5.1.0, tus ^5.1.1) across TWO majors (3→4→5). Attempted the family bump: tsc fallout is small (`uppy.close()`→`destroy()`; `upload-progress` payload fields now `number\|null`, which forces a null-mapping decision on the embed's public `onProgress` contract) — but reading v5 sources proved a silent visual break the gate cannot catch: v3's drag-over class `is-dragover` is renamed `uppy-DragDrop--isDraggingOver`, so widget.css's `.uppy-DragDrop--isDragDropSupported.is-dragover` rule would never match and the drop-zone highlight dies with zero test/hermetic signal (widget tests mock `buildUppy`; hermetic only greps external URLs). Release-critical widget + gate-invisible visual/DOM risk across two majors = not mechanical. Reverted cleanly. Routine currency work post-beta, with a manual visual pass. NOTE: root `@uppy/core→nanoid ^5.1.6` override is NOT dead weight at uppy 5 — core 5.2.0 ships `nanoid ^5.0.9`, the override still floors it (resolved 5.1.16); keep it. |

PRs #7 and #11 absorbed locally (commits on this branch; the PRs auto-close when the operator pushes main). #9 and #10 stay open as the post-beta currency backlog — home: "routine currency work post-beta" (deferral ledger, Task 15).
