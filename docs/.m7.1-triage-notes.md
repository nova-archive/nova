# P2-M7.1 triage notes (working scratch — Task 15 folds this into REVIEW_2026_07_04.md)

## GitHub-Actions Dependabot PRs (Task 4, absorbed locally 2026-07-04)

| PR | action old→new | taken/deferred | reason |
|----|----------------|----------------|--------|
| #1 | actions/setup-go v5→v6 (v6.5.0) | taken | Breaking changes are node24 runtime (CI-side, runner ≥ v2.327.1 on ubuntu-latest) + improved toolchain selection; `go-version-file: go.mod` + `cache` inputs unchanged. |
| #2 | actions/checkout v4→v7 (v7.0.0) | taken | v5/v6/v7 breaking notes: node24 runtime, creds persisted to a separate file (v6), fork-PR checkout blocked for `pull_request_target`/`workflow_run` (v7) — we only trigger on `push`/`pull_request`, unaffected. |
| #3 | docker/setup-buildx-action v3→v4 (v4.2.0) | taken | Major is a node24 runtime bump; no input changes; we pass no inputs. |
| #4 | golangci/golangci-lint-action v8→v9 (v9.3.0) | taken | v9 gate PASSED: v9.0.0 notes = node20→node24 runtime + additive features (install-only, Module Plugin System); no input removals; v2-format `.golangci.yml` (ours is `version: "2"`) still the supported config; `version: latest` input unchanged; Go version comes from setup-go (workflow uses `go-version-file`), unaffected. v9.1.0–v9.3.0 notes contain no breaking changes. |
| #5 | actions/setup-node v4→v6 (v6.4.0) | taken | v5 breaking = auto package-manager cache detection + node24; v6 breaking = auto-caching limited to npm. We pass `cache: npm` + `node-version-file: .nvmrc` explicitly, so behaviour is pinned regardless. |

All five absorbed as SHA pins with `# vX.Y.Z` comments (Task 4 also SHA-pinned the other 8 action repos at their current major's latest release). PRs #1–#5 auto-close when the operator pushes main.

## Go vulnerability alerts (Task 6, absorbed PR #13 locally 2026-07-05)

| alert | module old→patched→taken | verdict | reason/evidence |
|-------|--------------------------|---------|-----------------|
| x/image TIFF PackBits excessive resource consumption (HIGH, < 0.41.0) | golang.org/x/image v0.38.0→v0.41.0→**v0.43.0** | **unreachable, patched anyway** | `go mod why golang.org/x/image/tiff` → "main module does not need package"; module needed only via `golang.org/x/image/webp` imported by `internal/integration/m5_image_test.go` (test-only); govips does image work via libvips (cgo), its go.mod edge to x/image never reaches the tiff package. Policy: patch regardless. |
| x/net HTML parser DoS (MEDIUM, < 0.55.0) | golang.org/x/net v0.53.0 (indirect)→v0.55.0→**v0.55.0** | **reachable, patched** | `go mod why golang.org/x/net/html` → nova-image/internal/transform → govips/v2/vips → x/net/html/charset → x/net/html — production image-transform path does reach the parser (charset sniffing); no direct x/net import in Nova source. Bumped explicitly; stays indirect in go.mod. |

PR #13 (Go minor/patch group, 10 modules) absorbed with two holds: **kubo held at v0.41.0** (v0.42.0 requires `go 1.26.4`; go.mod pins 1.26.2 per toolchain policy) and **boxo held at v0.39.0** (boxo ≥0.40 forces go-libp2p-kad-dht v0.40.0, whose new provider `Stats(ctx)` API does not compile against kubo v0.41.0; kad-dht taken at v0.39.2, the newest compatible patch). Remaining 8 direct movements taken at PR versions (no newer same-minor patches existed as of 2026-07-05), except **x/crypto uplifted one further to v0.52.0**: govulncheck flagged five reachable x/crypto/ssh vulns at v0.51.0 (GO-2026-5013/5017/5018/5019/5020, all fixed in v0.52.0). Post-bump govulncheck: only GO-2024-3218 remains (kad-dht content censorship, **Fixed in: N/A** — no fixed release exists; pre-existing at v0.39.1, affects all versions; accepted/known, not introduced by this change). PR #13 auto-closes on push; the kubo/boxo group will re-open — defer to a milestone that lifts the go directive.
