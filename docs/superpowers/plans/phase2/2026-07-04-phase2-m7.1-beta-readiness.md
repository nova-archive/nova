# P2-M7.1 — Beta Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close all Phase-2 deferred debt under the renamed P2-M7.1 milestone and bring Nova to a polished, honestly-documented, vulnerability-free state for the first private beta.

**Architecture:** Rename P2-M6.1 → P2-M7.1 everywhere; fix partition maintenance at migration/runtime/test layers; land the below-floor replacement queue on the existing healing machinery (drain-pattern query classification); apply the coordinator→donor TLS identity fix to the donor↔donor repair client; absorb Dependabot PRs + alerts + Scorecard findings; run security/logic/performance review passes with fixes; sweep doc drift; rebuild the README.

**Tech Stack:** Go 1.26 / pgx / sqlc / goose-style SQL migrations / Postgres 16 partitioned tables / Vite 8 + Vitest 4 SPAs / GitHub Actions.

**Design doc:** `docs/superpowers/specs/phase2/2026-07-04-phase2-m7.1-beta-readiness-design.md` (decisions D-M7.1-1 … D-M7.1-8 — read it first; every task below implements a numbered decision).

## Global Constraints

- Branch: `p2-m7.1-beta-readiness` off `main`; local fast-forward merge + annotated tag `p2-m7.1-beta-readiness` at the end; **no remote push**.
- All subagents run on model **fable** (operator instruction).
- Never hand-edit `internal/db/gen/*` — change `internal/db/queries/*.sql` and run `make sqlc-generate`.
- Migrations are forward-only appends; `make migrations-frozen` must stay green (new files only).
- `gofmt` only files you touched. `golangci-lint` is CI-only; do not run locally.
- Donor dependency boundary: `make node-deps-check` green after every Go-touching task; `github.com/prometheus/*` stays hard-denied in the donor graph.
- Historical docs under `docs/superpowers/{specs,plans}` receive ONLY the P2-M6.1→P2-M7.1 pointer fix — no other edits.
- `docs/ROADMAP.md` is authoritative for milestone status; it is updated once, in the final task.
- Commit after every green step; message prefix `(P2-M7.1)` suffix convention as in recent history.

---

### Task 0: Branch + milestone docs

**Files:**
- Create: branch `p2-m7.1-beta-readiness`
- Add: `docs/superpowers/specs/phase2/2026-07-04-phase2-m7.1-beta-readiness-design.md`, `docs/superpowers/plans/phase2/2026-07-04-phase2-m7.1-beta-readiness.md` (already written)

- [ ] **Step 1:** `git checkout -b p2-m7.1-beta-readiness main`
- [ ] **Step 2:** `git add docs/superpowers/specs/phase2/2026-07-04-phase2-m7.1-beta-readiness-design.md docs/superpowers/plans/phase2/2026-07-04-phase2-m7.1-beta-readiness.md && git commit -m "docs(p2-m7.1): beta-readiness design + plan — rename P2-M6.1, absorb all deferred Phase-2 debt (P2-M7.1)"`

### Task 1: The rename — P2-M6.1 → P2-M7.1 (D-M7.1-1)

**Files (verified by grep, re-verify at execution):**
- Modify: `docs/ROADMAP.md`, `docs/superpowers/specs/phase2/2026-07-01-phase2-m7-production-hardening-release-design.md`, `docs/superpowers/plans/phase2/2026-07-01-phase2-m7-production-hardening-release.md`, `docs/runbooks/donor-lifecycle.md` (check), `cmd/novactl/node_drain.go`, `internal/audit/possession/verify.go`, `internal/audit/possession/trust_test.go`, `internal/metrics/collector.go`, `internal/db/queries/drain.sql`
- Regenerate: `internal/db/gen/drain.sql.go`, `internal/db/gen/querier.go` via `make sqlc-generate`

- [ ] **Step 1:** `grep -rn 'P2-M6\.1\|P2‑M6\.1' --include='*.md' --include='*.go' --include='*.sql' --include='*.sh' --include='*.ts' --include='*.tsx' . | grep -v node_modules` — enumerate every site. Distinguish Phase-1 `M6.1` (keystore, e.g. `docs/superpowers/specs/phase1/2026-05-31-m6.1-*`, `docs/REVIEW_2026_05_31.md` "M6.1/M6.2" lineage) — those are NOT touched.
- [ ] **Step 2:** Replace `P2-M6.1` → `P2-M7.1` at every Phase-2 site (docs + code comments + SQL comments). In prose that says "P2-M6.1/M8" keep the alternation as "P2-M7.1/M8".
- [ ] **Step 3:** `make sqlc-generate && git diff --stat internal/db/gen` — only comment changes appear.
- [ ] **Step 4:** `go build ./... && grep -rn 'P2-M6\.1' . --include='*.md' --include='*.go' --include='*.sql' | grep -v node_modules` → zero hits.
- [ ] **Step 5:** Commit: `docs(p2-m7.1): rename P2-M6.1 → P2-M7.1 across docs, code comments, and queries (P2-M7.1)`.

### Task 2: Partition provisioning — migration 0017 + Maintainer covers jobs (D-M7.1-2)

**Files:**
- Create: `internal/db/migrations/0017_partition_provisioning.sql`
- Modify: `internal/audit/integrity/retention.go` (maintain() + doc comments), `internal/db/migrations/MANIFEST.sha256` (per the migrations-frozen mechanism — inspect `make migrations-frozen` first to learn the update procedure)
- Test: `internal/audit/integrity/retention_test.go` (extend), `internal/db/migrations/partition_provisioning_test.go` (new, pattern: existing `*_state_test.go`)

**Interfaces:**
- Produces: migration 0017 (idempotent month-relative partitions for `jobs`, `integrity_audits`, `audit_log`); `Maintainer.maintain` additionally create-aheads `jobs`.

- [ ] **Step 1: Failing test** — new migration test asserting that, after `migrate up` on a fresh DB, an INSERT into each of `jobs`, `integrity_audits`, `audit_log` with `now()` timestamps succeeds, and partitions exist for current month + 2. Run: fails (no partition for 2026-07).
- [ ] **Step 2: Migration 0017** — goose up block:

```sql
-- +goose Up
-- +goose StatementBegin
-- Migration 0017: month-relative partition provisioning (P2-M7.1).
-- 0002/0003 committed fixed partitions ending 2026-07-01; a fresh install in
-- any later month was insert-broken, and nothing create-aheads jobs at
-- runtime. Provision current month + 2 for all three partitioned parents,
-- idempotently, using the retention.go naming/bounds convention.
DO $$
DECLARE
    parent  text;
    m       int;
    p_start timestamptz;
    p_end   timestamptz;
    p_name  text;
BEGIN
    FOREACH parent IN ARRAY ARRAY['jobs','integrity_audits','audit_log'] LOOP
        FOR m IN 0..2 LOOP
            p_start := date_trunc('month', now() AT TIME ZONE 'UTC') + make_interval(months => m);
            p_end   := p_start + interval '1 month';
            p_name  := format('%s_%s', parent, to_char(p_start, 'YYYY_MM'));
            EXECUTE format(
                'CREATE TABLE IF NOT EXISTS %I PARTITION OF %I FOR VALUES FROM (%L) TO (%L)',
                p_name, parent,
                to_char(p_start, 'YYYY-MM-DD') || ' 00:00:00+00',
                to_char(p_end,   'YYYY-MM-DD') || ' 00:00:00+00');
        END LOOP;
    END LOOP;
END $$;
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
-- Forward-only: partitions are data-bearing; down is a no-op by design.
SELECT 1;
-- +goose StatementEnd
```

(Adjust goose markers/Down conventions to match neighboring migrations exactly — read `0016_node_draining.sql` first.)
- [ ] **Step 3:** Update the frozen-migrations manifest by the sanctioned procedure only (read the Makefile target; if it verifies existing hashes and appends new files freely, nothing manual is needed).
- [ ] **Step 4:** Extend `retention.go`: in `maintain()` add `jobs` create-ahead before the others (job queue is the hottest insert path):

```go
	// jobs is monthly-partitioned too (0002_jobs.sql); the "partition-rotation
	// job" that comment promised is this Maintainer (P2-M7.1). Create-ahead
	// only — the job reaper owns row lifecycle, so there is no prune pass.
	if err := m.ensureMonthlyPartitions(ctx, "jobs"); err != nil {
		m.log.WarnContext(ctx, "jobs: ensure partitions", "err", err)
	}
```

Also update the stale `Maintainer` doc comment ("committed partitions stop at 2026-07-01" → note 0017 provisions install-time months and the Maintainer sustains all three parents).
- [ ] **Step 5: Failing test for the Maintainer** — extend the retention test: with a clock in month X, run `maintain`, assert `jobs_YYYY_MM` exists for X..X+2. Run → fails → passes after Step 4.
- [ ] **Step 6:** `make migrate-up && make smoke && go test ./internal/audit/integrity/... ./internal/db/... && make migrations-frozen` all green.
- [ ] **Step 7:** Commit: `fix(p2-m7.1): month-relative partition provisioning — migration 0017 + Maintainer create-ahead for jobs (P2-M7.1)`.

### Task 3: dbtest fail-fast + suite verification (D-M7.1-2 layer 3)

**Files:**
- Modify: `internal/dbtest/dbtest.go` (cleanup ordering / bounded close at line ~56 `t.Cleanup(pool.Close)`)
- Test: reproduce first — a scratch test that opens a tx and `t.Fatal`s

- [ ] **Step 1:** Reproduce the 15-minute hang mechanism (leaked tx on `t.Fatal` → `pool.Close` blocks). Identify which two packages hung and why their cleanup ordering leaks.
- [ ] **Step 2:** Fix in `dbtest` so `pool.Close` cannot block unboundedly (goroutine + `select` with ~5s grace and a loud `t.Log` on timeout, or guarantee tx/conn release cleanups run before pool close). Keep the fix in the harness, not per-test.
- [ ] **Step 3:** Verify: the scratch reproduce-test now fails in seconds; delete the scratch test.
- [ ] **Step 4:** `make test` — full suite. Expected: the 9 previously-failing packages are green (partition fix); no hangs. Record runtimes.
- [ ] **Step 5:** Commit: `fix(p2-m7.1): dbtest pool close is bounded — a failing test fails fast, never hangs 15m (P2-M7.1)`.

### Task 4: Workflow hygiene — Actions majors + SHA pinning + token permissions (D-M7.1-5)

**Files:**
- Modify: `.github/workflows/ci.yml`, `.github/workflows/codeql.yml`, `.github/workflows/scorecard.yml`

- [ ] **Step 1:** For each `uses:` (17 distinct actions), resolve the target version — absorb PRs #1–#5 (setup-go v6, checkout latest major, buildx v4, setup-node v6, golangci-lint-action v9) — and pin to the full commit SHA with a `# vX.Y.Z` comment. Resolve SHAs via `gh api repos/<owner>/<repo>/git/ref/tags/<tag>`.
- [ ] **Step 2:** golangci-lint-action v9 gate: confirm v9 supports the v2 `.golangci.yml` + `go-version-file` inputs (read its release notes via `gh api`). If incompatible, stay on v8 SHA-pinned and record the verdict for the triage table (Task 15 collects it).
- [ ] **Step 3:** Add top-level `permissions: contents: read` to `ci.yml` and `codeql.yml`; keep/verify job-level widening (attestations, security-events, packages) is the minimal set per job.
- [ ] **Step 4:** Validate: `actionlint` if available (`command -v actionlint`), else careful YAML re-read; CI cannot run locally — note that the first push validates.
- [ ] **Step 5:** Commit: `build(p2-m7.1): absorb Actions major bumps, SHA-pin all workflow actions, least-privilege token permissions (P2-M7.1)`.

### Task 5: Docker base-image digest pinning (D-M7.1-5)

**Files:**
- Modify: `docker/coordinator.Dockerfile` (3 FROMs), `docker/node.Dockerfile` (2 FROMs)

- [ ] **Step 1:** Resolve current digests: `docker buildx imagetools inspect <image:tag>` (or `gh api` / `crane digest` fallback) for `golang:1.26-bookworm`, `gcr.io/distroless/static-debian12:nonroot`, `node:22-bookworm`, `debian:bookworm-slim`.
- [ ] **Step 2:** Rewrite each `FROM image:tag` → `FROM image:tag@sha256:… # tag as of 2026-07-04`; keep stage names.
- [ ] **Step 3:** `docker build -f docker/coordinator.Dockerfile .` and node equivalent (or `make docker-build`) — both build.
- [ ] **Step 4:** Commit: `build(p2-m7.1): pin Docker base images by digest (P2-M7.1)`.

### Task 6: Go dependency bumps + vuln closure (D-M7.1-5)

**Files:**
- Modify: `go.mod`, `go.sum`

- [ ] **Step 1:** Read PR #13's version movements: `gh pr diff 13 -- go.mod | head -80`. Apply the same bumps locally via `go get module@version` for each, **plus** whatever versions clear the two Go alerts (`golang.org/x/image` TIFF PackBits ≥ fixed version per `gh api` the alert's `security_vulnerability.first_patched_version`; `golang.org/x/net` likewise). `go mod tidy`.
- [ ] **Step 2:** `go build ./... && make test && make node-deps-check && make migrations-frozen && make codegen-check` — all green.
- [ ] **Step 3:** Confirm alert closure locally: `go list -m golang.org/x/image golang.org/x/net` ≥ first-patched versions.
- [ ] **Step 4:** Commit: `build(p2-m7.1): go minor/patch bumps — closes x/image TIFF + x/net alerts (P2-M7.1)`.

### Task 7: npm minor/patch bumps + vuln closure (D-M7.1-5)

**Files:**
- Modify: `package.json`, `package-lock.json` (workspaces root), possibly `web/*/package.json`

- [ ] **Step 1:** Read PR #14's movements (`gh pr diff 14 -- package.json web/admin/package.json web/setup/package.json web/widget/package.json | head -120`); apply the same minor/patch bumps.
- [ ] **Step 2:** Clear the 6 npm alerts: `npm audit` → bump transitive `undici` (root lock has `^7.25.0`; move within 7.x to the first-patched version — check each alert's patched version) and `esbuild` (via vite/toolchain patch bump). Use `npm audit fix` (never `--force`) + targeted `overrides` only if a parent won't move; respect the existing `nanoid` override.
- [ ] **Step 3:** `make web` (admin + widget + setup: install, lint, test, build, hermetic gates) — green. `npm audit --omit=dev` and `npm audit` → zero known vulnerabilities (dev-only esbuild acceptable if unfixable without majors — record verdict).
- [ ] **Step 4:** Commit: `build(p2-m7.1): npm minor/patch bumps — closes undici + esbuild alerts (P2-M7.1)`.

### Task 8: npm major-version triage — verdict per PR (D-M7.1-5)

**Files:**
- Possibly modify: `web/admin/**`, `web/setup/**`, `web/widget/**`
- Produce: verdict lines for the triage table (handed to Task 15)

For each of #7 (typescript 6.0.3), #9 (@uppy/drag-drop 5.1.0), #10 (@tanstack/react-query 5.101.0), #11 (react-router-dom 7.17.0):

- [ ] **Step 1:** Attempt the bump in isolation (one commit each). Gate: `make web` fully green AND the migration is mechanical (renames/codemods, no behavioral redesign). TypeScript first (dev-only, likely cheap); uppy last (widget is release-critical, 3→5 crosses two majors and touches the `nanoid` override).
- [ ] **Step 2:** Take it (commit `build(p2-m7.1): take <pkg> vN — mechanical migration, web gate green (P2-M7.1)`) or revert cleanly and write the verdict: `deferred — <one-line reason>, home: routine currency work post-beta`.
- [ ] **Step 3:** Record all four verdicts in a note file for Task 15: `docs/.m7.1-triage-notes.md` (deleted after Task 15 folds it in).

### Task 9: Donor↔donor repair-client TLS identity verification (D-M7.1-4)

**Files:**
- Modify: `internal/node/agent/client.go` (repair-fetch TLS config)
- Reuse: `internal/federation/transport/identity.go` (the M7 coordinator→donor fix — read `bb31141` first: `git show bb31141`)
- Test: alongside existing transport/agent tests; both directions

- [ ] **Step 1:** Read the M7 fix (`git show bb31141 -- internal/federation/transport/identity.go`) and the current donor repair client's TLS setup. Confirm the defect: hostname/ServerAuth verification (or weaker) instead of federation-CA + `nova://` URI-SAN identity.
- [ ] **Step 2: Failing test** — donor repair client connecting to a TLS server presenting a valid-federation-CA cert with the WRONG node identity in the URI SAN must refuse; correct identity must succeed. Mirror the M7 fix's test structure.
- [ ] **Step 3:** Apply the same verification path (`transport.identity` verify func) to the donor's repair client config; no hostname fallback.
- [ ] **Step 4:** `go test ./internal/node/... ./internal/federation/... && make node-deps-check` green. Run the federation e2e package: `go test ./internal/federation/e2e/...`.
- [ ] **Step 5:** Amend `docs/specs/FEDERATION_PROTOCOL.md` transport-security section: donor↔donor repair now verifies peer identity identically to coordinator→donor (one paragraph, owning-milestone convention).
- [ ] **Step 6:** Commit: `fix(p2-m7.1): donor↔donor repair client verifies federation identity — CA chain + nova:// URI SAN, both directions tested (P2-M7.1)`.

### Task 10: Below-floor schema + query classification (D-M7.1-3)

**Files:**
- Create: `internal/db/migrations/0018_below_floor.sql`
- Modify: `internal/db/queries/*.sql` (the safety-count/placement/selection queries touched by M7's drain classification — find via `grep -rn 'draining_at' internal/db/queries/`), then `make sqlc-generate`
- Test: `internal/db/migrations/below_floor_state_test.go` (pattern: `draining_state_test.go`), query-level tests beside the drain ones (`internal/orchestrator/drain_test.go` shows the harness)

- [ ] **Step 1:** Migration 0018: `ALTER TABLE nodes ADD COLUMN below_floor_since timestamptz;` + partial index `nodes_below_floor_idx ON nodes (below_floor_since) WHERE below_floor_since IS NOT NULL` + a `-- +goose Down` no-op mirroring 0016/0017 convention. Migration test per `draining_state_test.go` pattern (column exists, register/heartbeat never touch it).
- [ ] **Step 2:** Query classification, mirroring drain exactly (read the M7 design § D-M7-6 sort-key text quoted in `2026-07-01-…-design.md:420-436`): safety counts (`healthy_acked`, `sourceable_acked`, `CountSourceableHolders`) EXCLUDE nodes where `below_floor_since IS NOT NULL AND below_floor_since <= now() - grace` (grace as a query arg); placement excludes the same; selection queries append `(n.below_floor_since IS NOT NULL)` to the prepended sort key AFTER the draining term.
- [ ] **Step 3:** `make sqlc-generate`; unit tests: a sustained-below-floor holder drops out of safety counts but still appears (last) in repair-source selection.
- [ ] **Step 4:** `make bench-corpus-explain` — the EXPLAIN gate still passes with the modified queries (index availability, incl. the new partial index if the planner needs it).
- [ ] **Step 5:** Commit: `feat(p2-m7.1): below-floor sustained marker (0018) + drain-pattern query classification (P2-M7.1)`.

### Task 11: Below-floor sweep, config, observability, spec amendments (D-M7.1-3)

**Files:**
- Modify: `internal/orchestrator/liveness.go` / `orchestrator.go` (marker maintenance + the new healing pass ordered after `donor_lost` and `tier1` — read `scheduler.go` tick structure first), `internal/config/**` (`below_floor_replacement` block: `enabled` default true, `hysteresis_margin` 0.05, `grace` 24h, `requeue_batch` 500), `/settings` knob registry (find via the M6 possession knobs: `grep -rn possession_audit internal/config internal/api`), `internal/metrics/collector.go` (`nova_below_floor_nodes` scrape-time family + requeue counter via observer seam)
- Modify: `docs/specs/HEALING_PROTOCOL.md` (below-floor pass section), `docs/runbooks/donor-lifecycle.md` (rewrite § below-floor debt: automated + knobs + when to intervene)
- Test: `internal/orchestrator/below_floor_test.go` (pattern: `drain_test.go` + `drills_test.go`)

Behavioral contract (tests to write BEFORE implementation, one commit per red/green cycle):
1. Marker: sweep sets `below_floor_since` when `reputation_score < reputation_floor`; clears only at `score ≥ floor + hysteresis_margin`; wobble inside the band never clears/resets.
2. Grace: no requeue before `grace` elapses; counts flip only for sustained nodes (Task 10 queries take grace as arg — wire the config value).
3. Replacement: sustained node's CIDs get `MarkReplicationDirtyForNode` + `EnqueueReconcileForNode(reason='below_floor')`, capped at `requeue_batch` per tick, AFTER donor_lost and tier1 passes.
4. Replace-then-demote: assignment on the below-floor node is failed with reason `below_floor_replaced` only once the blob's healthy count is back at target; sole-holder case keeps it as repair source (assert no durability dip, mirroring the drain SOLE-holder e2e).
5. Disabled: `enabled=false` → marker maintenance still runs (observability), zero requeues/demotions.
6. Metrics: `nova_below_floor_nodes` present coordinator-side; requeue counter increments; donor graph prometheus-free (`make node-deps-check`).

- [ ] **Step 1–N:** Red/green per contract item above, then wire config + `/settings` knobs (`enabled`, `grace`), then metrics, then the two doc amendments.
- [ ] **Final steps:** `go test ./internal/orchestrator/... ./internal/metrics/... ./internal/federation/e2e/...` green; `make test` green; commit series `feat(p2-m7.1): below-floor replacement — <slice> (P2-M7.1)`.

### Task 12: Security review pass (D-M7.1-6)

**Scope (priority order):** `internal/envelope`, `internal/masterkey`, `internal/secret`, `internal/auth/**`, signed-URL path (`internal/api` + `docs/specs/SIGNED_URL_FORMAT.md` conformance), `internal/federation/{transport,tokens,ca,replay}`, `internal/api` (authz gates, CORS from M0.3, rate limits, metrics listener exposure), `internal/upload`.

- [ ] **Step 1:** Dispatch a review subagent (fable) per the scope list with explicit instructions: verify each spec promise (ENCRYPTION_ENVELOPE, SIGNED_URL_FORMAT, FEDERATION_PROTOCOL, THREAT_MODEL) against the implementation; hunt authz bypasses, timing/misuse of crypto primitives, token-scope confusion, replay windows, SSRF in donor-URL handling, secrets in logs/errors.
- [ ] **Step 2:** Triage findings: fix P0/P1 in-milestone (TDD, one commit per fix), defer P2+ with named homes. Every finding → `docs/REVIEW_2026_07_04.md` (Task 15 assembles; write findings to `docs/.m7.1-review-security.md` meanwhile).
- [ ] **Step 3:** `make test` green after fixes; commits `fix(p2-m7.1): <finding> (P2-M7.1)`.

### Task 13: Logic/concurrency review pass (D-M7.1-6)

**Scope:** `internal/orchestrator` (liveness × healing × drain × NEW below-floor interplay), `internal/audit/possession` (dispatcher, trust machine, tx outcomes), `internal/jobs` (leasing/reclaim/backoff), lifecycle sweeps (`internal/lifecycle`, moderation sweep), `internal/upload` session state machine.

- [ ] **Step 1:** Dispatch a review subagent (fable): races, lost updates (`FOR UPDATE` discipline), tx boundary bugs, idempotency of one-shot ops, partial-failure recovery, pathological interleavings between the three node-state axes (draining / below-floor / liveness).
- [ ] **Step 2:** Same triage/fix/record protocol as Task 12 (`docs/.m7.1-review-logic.md`).

### Task 14: Performance review pass (D-M7.1-6)

- [ ] **Step 1:** Dispatch a review subagent (fable): hot queries vs the EXPLAIN-gate list (`internal/benchcorpus`), N+1 patterns, pool acquisition discipline, sweep batch sizing (incl. new below-floor pass at corpus scale), SPA bundle sanity (`make admin-build` output sizes vs previous).
- [ ] **Step 2:** Run `make bench-corpus-ci` and `make bench-corpus-explain`; compare against `reports/benchmarks/p2-m7-corpus-2026-07-04.md` thresholds. Regressions are P1 findings.
- [ ] **Step 3:** Same triage/fix/record protocol (`docs/.m7.1-review-perf.md`).

### Task 15: Doc-drift sweep + deferral ledger + REVIEW doc (D-M7.1-7)

**Files:**
- Modify: every living markdown with drift (top-level `*.md`, `docs/*.md`, `docs/{specs,legal,recipes,runbooks,quickstart}/**`, `deploy/operator/README.md`, `.github/*.md`, `simulations/**.md`)
- Create: `docs/REVIEW_2026_07_04.md`
- Delete: `docs/.m7.1-review-*.md`, `docs/.m7.1-triage-notes.md` after folding in

- [ ] **Step 1:** Dispatch drift-check subagents (fable) in batches by directory; each verifies every normative claim (commands, paths, env vars, defaults, endpoint lists, migration numbers, port numbers, milestone statuses) against the code and fixes in place. Known seeds: `docs/images/quickstart/README.md` says screenshots are uncaptured (they exist since 2026-06-14 — rewrite as a regeneration guide); dual THREAT_MODEL (`docs/THREAT_MODEL.md` vs `docs/specs/THREAT_MODEL.md`) → make one canonical, the other a pointer stub (check inbound links first); `docs/quickstart.md` + `docs/quickstart/donor.md` command-by-command re-verification.
- [ ] **Step 2:** Deferral-ledger audit: walk every `Deferrals:` clause in `docs/ROADMAP.md` Phase-2 rows + Phase-1 rows naming "Phase 2" as home; assign each a disposition (`shipped-in-<tag>` / `absorbed by P2-M7.1` / `moot (reason)` / `re-deferred → <home>`). Verify the expected-moot M10 "cross-node rotation propagation" claim (donors hold ciphertext only — confirm no wrapped-DEK material crosses the federation boundary) before recording it.
- [ ] **Step 3:** Assemble `docs/REVIEW_2026_07_04.md` in the `REVIEW_2026_05_31.md` lineage: summary, security/logic/perf findings tables (severity, disposition, commit), dependency-triage table (M14 format — locate it via `grep -rn -i 'triage' docs/superpowers/specs/phase1/2026-06-09-phase1-m14-*`), drift-fix list, deferral ledger.
- [ ] **Step 4:** Commit: `docs(p2-m7.1): REVIEW_2026_07_04 — beta-readiness audit, drift sweep, deferral ledger (P2-M7.1)`.

### Task 16: README revamp + brand assets (D-M7.1-8)

**Files:**
- Create: `docs/images/brand/` (logo/wordmark SVG extracted from `docs/design/Nova Brand _standalone_.html` — inline SVG expected; extract, don't screenshot), `docs/development.md` (receives the old README's dev walkthrough + MCP section, content moved intact)
- Rewrite: `README.md` from scratch per the D-M7.1-8 section order
- Modify: any doc that linked to moved README anchors (`grep -rn 'README.md#' docs/ web/ --include='*.md'`)

- [ ] **Step 1:** Extract brand assets; verify the SVG renders (open in browser or `rsvg-convert`-check if available). If no clean SVG exists in the HTML, fall back to a typographic wordmark using the brand palette (`--nova-deep #B83A18`) — do not fabricate a logo.
- [ ] **Step 2:** Move dev walkthrough + MCP section to `docs/development.md` verbatim (then re-verify its commands against Task 15's findings).
- [ ] **Step 3:** Write the new README per D-M7.1-8: logo → one-paragraph pitch → real badges only (CI, CodeQL, Scorecard, Apache-2.0, latest tag) → hero screenshot (`docs/images/quickstart/05-live.png` or `04-review.png`) → who-for → features (each linking its spec/doc) → architecture diagram + prominent trust-model note → progress table → three quickstarts → doc map → contributing/security/license. Every claim cross-checked against ROADMAP + code; status wording matches the post-M7.1 reality.
- [ ] **Step 4:** Link check: every relative link in the new README resolves (`grep -oE '\]\([^)]+\)' README.md` + existence check).
- [ ] **Step 5:** Commit: `docs(p2-m7.1): README rebuilt — brand assets, honest status, three onboarding paths (P2-M7.1)`.

### Task 17: ROADMAP row + final gates + merge + tag

**Files:**
- Modify: `docs/ROADMAP.md` (new P2-M7.1 row, in the established row style, marked ✅ Implemented with tag; P2-M7 row's deferral pointers already renamed by Task 1), master-plan status flips if the phase-2 master plan doc tracks milestones (check `docs/superpowers/plans/phase2/2026-06-11-phase2-federation.md`)

- [ ] **Step 1:** Write the P2-M7.1 ROADMAP row: rename note, partition fix (0017), below-floor queue (0018 + drain-pattern classification + sweep), donor↔donor TLS, dependency/alert closure (9→0), Scorecard hardening, review passes + REVIEW doc, README rebuild. Deferrals out of M7.1 restated with homes.
- [ ] **Step 2:** Full release gate: `make test && make web && make smoke && make migrations-frozen && make codegen-check && make node-deps-check` — all green, outputs captured.
- [ ] **Step 3:** `gofmt -l` on touched Go files → empty.
- [ ] **Step 4:** Commit ROADMAP; `git checkout main && git merge --ff-only p2-m7.1-beta-readiness && git tag -a p2-m7.1-beta-readiness -m "P2-M7.1: beta readiness — deferred-debt closure & release polish"`.
- [ ] **Step 5:** Update auto-memory (phase-2 plan status, partition bug resolved). Final report to the operator: per-PR dispositions (which Dependabot PRs auto-close on push, which majors were taken/deferred and why), alert closure evidence, review-finding summary, anything requiring operator action (push, PR comments, screenshot regeneration none-needed, brand-HTML commit decision).

## Self-review notes

- Spec coverage: D-M7.1-1→Task 1; -2→Tasks 2–3; -3→Tasks 10–11; -4→Task 9; -5→Tasks 4–8; -6→Tasks 12–14; -7→Task 15; -8→Task 16; release gate→Task 17. Rename precedes everything that re-touches those files; partition fix precedes all `make test` gates; features precede reviews; reviews precede the REVIEW doc; README last before gates.
- Placeholder scan: Tasks 10–14 intentionally carry behavioral contracts + exact file/command pointers rather than final Go code — the queries and orchestrator internals must be read in-task (the M7 drain implementation is the explicit template throughout). No "TBD"/"handle edge cases" steps remain.
- Type consistency: reason string `below_floor` (queue) vs `below_floor_replaced` (assignment failure) used consistently in Tasks 10–11; config block name `below_floor_replacement` matches design D-M7.1-3; migration numbers 0017 (partitions) / 0018 (below-floor) consistent everywhere.
