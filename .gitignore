# Build artifacts
/bin/
/dist/
/build/
/coordinator
/novactl
/node
/setup-wizard
coverage.out
coverage.html

# Generated code: sqlc output (internal/db/gen) is committed as of M3 so the
# build works without sqlc installed and the codegen-check gate detects drift.
# oapi-codegen output stays generated-and-ignored until M4 adopts it.
internal/api/codegen/

# Node
node_modules/
web/admin/dist/
web/widget/dist/
web/setup/dist/

# Local environment
.env
docker/.env
docker/docker-compose.override.yml
nova-config/
nova-secrets/
nova-postgres-data/
nova-kubo-data/
nova-tmp-uploads/

# Editor/OS
.DS_Store
.idea/
.vscode/
*.swp
